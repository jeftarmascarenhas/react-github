import React from 'react';

import './banner-events.css';

const BannerEvents = ({ events }) => (
  <div className="events-banner">
    <h1>Eventos de tecnologias</h1>
  </div>
);

export default BannerEvents;